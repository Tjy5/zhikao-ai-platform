# 运行时核心依赖（pip-compile 输入）
# 如需升级，请先在此文件修改版本/范围，再运行 `make lock`

# Web 框架
fastapi==0.115.5
uvicorn[standard]==0.30.1

# 数据访问
sqlalchemy==2.0.23
alembic==1.12.1

# 数据模型/配置
pydantic==2.8.2
python-multipart==0.0.9
python-dotenv==1.0.1

# 安全/认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt<5
email-validator==2.1.0.post1

# AI/文本处理
openai>=1.0.0
